// given a string, find all subsets
#include<iostream>
#include<cstring>
using namespace std;
/* 
    1. isolate the string from the first character and find all subsets of the remaining word using recursion
    2. combine all obtained subsets with 'a' to produce half the list
    3. combine all obtained subsets with '' to produce the other half
*/
int main()
{
    
}
