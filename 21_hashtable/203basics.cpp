/*
- functions:
    insertion
    deletion
    searching

- points
    - hashtable is an array of fixed size, which can grow in future.
    - array elements are indexed by a key, which is mapped to an array index (0 to table_size - 1)
    - mapping (using hash functon)
        - from key to index
        - h(input) = output
            - input - integer, float, string, etc.
            - output - index
              ex: h("john") = 12
*/
void hashfunction()
{
    /*
        - converts key (string) to address (int)
        - takes object of particular type
    */
}